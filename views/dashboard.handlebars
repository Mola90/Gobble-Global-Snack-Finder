<div class="w-screen flex flex-col items-center">

    {{!-- User Profile Section --}}
    <div class="container shadow-lg bg-stone-100 w-10/12 p-8 flex flex-row mt-8 justify-between">
        <div class="flex justify-self-start flex-row items-center">
            <div>
                <img class="shadow-md h-36 w-36 rounded-full bg-stone-100" src={{profile_picture}}>
            </div>
            <div>
                <h2 class="text-2xl ml-8">{{username}}</h2>
                <p class="ml-8 flex flex-row items-center">From {{user_country}} <span class="text-3xl ml-4">{{country_emoji}}</span></p>
            </div>
            
        </div>
        
        {{!-- Dashboard Navigation Section --}}
        <div id="stat-board" class="flex flex-row justify-self-end items-center ">
            <div class="flex flex-col m-8 items-center">
                <h3 class="font-bold text-2xl">{{numWishlist}}</h3>
                <p class="">Wishlist</p>
            </div>
            <div class="flex flex-col m-8 items-center">
                <h3 class="font-bold text-2xl">{{numRatings}}</h3>
                <p>Reviews</p>
            </div>
            <div class="flex flex-col m-8 items-center">
                <h3 class="font-bold text-2xl">{{numLikes}}</h3>
                <p>Likes</p>
            </div>
            <div class="flex flex-col m-8 items-center">
                <h3 class="font-bold text-2xl">{{submittedSnacks}}</h3>
                <p>Submissions</p>
            </div>
        </div>
    </div>
    
    {{!-- Commented this out to trial the cards still work in progress -SS --}}
    {{!-- <div id="dashboardNav" class="flex flex-row b w-10/12 mt-8 justify-center h-12 bg-stone-100 shadow-lg items-center">
        <ul class="flex space-x-12">
            <a href ="/dashboard/wishlist">WishList</a>
            <a href="/dashboard/review">Reviews</a>
            <a href="/dashboard/likes">Likes</a>
            <a href="/dashboard/submission">Submissions</a>
            <a href="/dashboard/edit">Edit Profile</a>
        </ul>
    </div> --}}

</div>
<body>

<!-- Cards Section -->
<div class="mt-8 grid grid-cols-4 gap-30">
    <!-- Wishlist Card -->
    <div class="bg-gray-50 border border-gray-300 rounded-lg shadow-md p-6 mb-15 h-23 w-23">
        <h3 class="text-lg font-semibold mb-4">
            <a href="/dashboard/wishlist" class="text-blue-500 hover:underline mt-2">Wishlist</a>
        </h3>
        <ul>
                <li>
                    <button onclick="editWish({{wish.id}})">Edit</button>
                    <button onclick="deleteWish({{wish.id}})">Delete</button>
                    <button onclick="updateWish({{wish.id}})">Update</button>
                </li>
        </ul>
    </div>

    <!-- Reviews Card -->
    <div class="bg-gray-50 border border-gray-300 rounded-lg shadow-md h-23 w-23 p-6 mb-15">
        <h3 class="text-lg font-semibold mb-4">
            <a href="/dashboard/review" class="text-blue-500 hover:underline">Reviews</a>
        </h3>
        <ul>
                <li>
                    <button onclick="edit({{wish.id}})">Edit</button>
                    <button onclick="delete({{wish.id}})">Delete</button>
                    <button onclick="update({{wish.id}})">Update</button>
                </li>
        </ul>
    </div>

    <!-- Likes Card -->
    <div class="bg-gray-50 border border-gray-300 rounded-lg shadow-md h-23 w-23 p-6 mb-15">
        <h3 class="text-lg font-semibold mb-4">
            <a href="/dashboard/likes" class="text-blue-500 hover:underline mt-2">Likes</a>
        </h3>
        <ul>
                <li>
                    <button onclick="edit({{wish.id}})">Edit</button>
                    <button onclick="delete({{wish.id}})">Delete</button>
                    <button onclick="update({{wish.id}})">Update</button>
                </li>
        </ul>
    </div>

    <!-- Submissions Card -->
    <div class="bg-gray-50 border border-gray-300 rounded-lg shadow-md h-23 w-23 p-6 mb-15">
        <h3 class="text-lg font-semibold mb-4">
            <a href="/dashboard/submission" class="text-blue-500 hover:underline mt-2">Submissions</a>
        </h3>
        <ul>
                <li>
                    <button onclick="edit({{wish.id}})">Edit</button>
                    <button onclick="delete({{wish.id}})">Delete</button>
                    <button onclick="update({{wish.id}})">Update</button>
                </li>
        </ul>
    </div>
</div>

</body>